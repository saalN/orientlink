# ==============================================================================
# Environment Variables for OrientLink Application
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in all required values (marked with *)
# 3. NEVER commit .env to version control (already in .gitignore)
# 
# SECURITY: Use strong, unique passwords and keep your API keys secret
# ==============================================================================

# ------------------------------------------------------------------------------
# Spring Configuration
# ------------------------------------------------------------------------------
# Environment profile: dev (development), prod (production), test (testing)
SPRING_PROFILES_ACTIVE=prod

# ------------------------------------------------------------------------------
# PostgreSQL Database Configuration
# ------------------------------------------------------------------------------
# * REQUIRED - PostgreSQL database credentials
# Use strong passwords in production (16+ chars, mixed case, numbers, symbols)
POSTGRES_DB=orientlink
POSTGRES_USER=orientlink_user
POSTGRES_PASSWORD=CHANGE_ME_use_strong_password_here

# ------------------------------------------------------------------------------
# JPA/Hibernate Configuration
# ------------------------------------------------------------------------------
# DDL Auto mode:
#   - validate: Validate schema (recommended for production)
#   - update: Auto-update schema (use in development only)
#   - none: No schema management
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
SPRING_JPA_SHOW_SQL=false

# ------------------------------------------------------------------------------
# OpenAI API Configuration
# ------------------------------------------------------------------------------
# * REQUIRED - Get your API key from: https://platform.openai.com/api-keys
# Keep this secret and never commit to version control
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE
OPENAI_MODEL=gpt-4o
OPENAI_TIMEOUT_SECONDS=60

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
# Application log level: TRACE, DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# SQL query log level (set to DEBUG to see queries)
SQL_LOG_LEVEL=WARN
